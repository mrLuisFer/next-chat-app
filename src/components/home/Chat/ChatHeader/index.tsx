import { Box } from "@chakra-ui/react";

export default function ChatHeader() {
  return (
    <Box
      as="header"
      boxShadow="0 2px 6px rgba(0, 0, 0, 0.5)"
      padding="1rem"
      m="0"
    >
      Header
    </Box>
  );
}
